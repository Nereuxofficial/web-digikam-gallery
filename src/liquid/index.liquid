<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Images</title>
    <!-- Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css" rel="stylesheet">
    <!-- No Search engine indexing -->
    <meta name="robots" content="noindex nofollow">
</head>

<body>
<!-- Images -->
<div class="container mx-auto">
    <div class="flex flex-wrap -mx-2">
        {% for image in images %}
        <div class="w-1/3 px-2">
            <div class="bg-white rounded-lg shadow-lg">
                <img src="image/{{ image.name }}" class="rounded-t-lg">
                <div class="p-4">
                    {% for tag in image.tags %}
                        <!-- Tag with link to searching that name -->
                    <a href="?filter={{ tag }}" class="inline-block bg-green rounded-full px-3 py-1 text-sm font-semibold text-grey-darker mr-2 mb-2">{{ tag }}</a>
                    {% endfor %}
                </div>
            </div>
            </div>
        {% endfor %}
    </div>
</div>

{{ bottom_text }}

</body>
